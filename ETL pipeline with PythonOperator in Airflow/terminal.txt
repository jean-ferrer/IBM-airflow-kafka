1. Open a terminal and create a directory structure for the staging area as follows: "/home/project/airflow/dags/python_etl/staging"

sudo mkdir -p /home/project/airflow/dags/python_etl/staging


2. Execute the following command to give appropriate permission to the directories.

sudo chmod -R 777 /home/project/airflow/dags/python_etl


3. Run the commands below to submit the DAG that was previously created.

export AIRFLOW_HOME=/home/project/airflow
cp ETL_toll_data.py $AIRFLOW_HOME/dags
